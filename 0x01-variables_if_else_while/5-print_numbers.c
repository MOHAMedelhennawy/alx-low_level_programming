#include <stdio.h>

/**
 *  * main - prints the alphabet in lowercase,
 *   * followed by a new line
 *    * Return: Always 0 (Success)
 *     */
int main(void)
{
	int i;

	for (i = 0; i<10; i++)
		printf("%d", i);
	return (0);
}
