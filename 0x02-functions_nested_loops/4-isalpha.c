#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 90)
		_putchar('1');
	if (c => 97 && c <= 122)
		_putchar('1');
	else
		_putchar('0');

}
